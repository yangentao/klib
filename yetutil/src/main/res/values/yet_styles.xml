<resources>

    <!-- Base application theme. -->
    <style name="yetTheme" parent="android:Theme.DeviceDefault.Light.NoActionBar">
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="yetFullScreen" parent="yetTheme">
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="yetTheme.Translucent" parent="yetTheme">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <style name="yetFullScreen.Translucent" parent="yetFullScreen">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>
</resources>
